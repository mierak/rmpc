---
title: Try without installing
description: Try rmpc now. Without installing.
---

import { Steps } from "@astrojs/starlight/components";
import { Code } from "@astrojs/starlight/components";

export const site = import.meta.env.SITE;
export const base = import.meta.env.BASE_URL;
export const url = `${site}${base}/try`;
export const cmd = `sh <(curl -sL ${url})`;

You can try rmpc without installing. Simply run the command below in your terminal.
Credits to zellij for this idea!

<div style={{}}>
    <Code code={cmd} lang="bash" frame="none" showLineNumbers={false} />
</div>

:::caution
Do not run unknown commands from the internet. Always check the source code yourself.
You can find the source code of the script that will be executed <a href={url}>here</a>.
:::
